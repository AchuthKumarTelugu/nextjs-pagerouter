/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output:"export",//to get out folder
  // images: {   // to un optimised images
  //   unoptimized: true
  // },
  // distDir: 'dist',
  // assetPrefix: '/dist/',//to make js and css files work in static site,
  // trailingSlash:true// to prevent loss of data on reloading
};



export default nextConfig;
